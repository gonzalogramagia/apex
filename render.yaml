services:
  - type: static
    name: apex-private
    buildCommand: npm run build
    publishPath: dist
    headers:
      - path: /assets/*
        headers:
          - key: Cache-Control
            value: public, max-age=31536000, immutable
    routes:
      - source: /([^.]*)
        destination: /index.html
        type: rewrite
