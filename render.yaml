services:
  - type: web
    name: apex-private
    env: static
    buildCommand: npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
